import * as directionKeys from './direction-keys';
import * as moduleHot from './module-hot';
import * as immutable from './immutable';
import * as scaleBody from './scale-body';
import * as statsCounter from './stats-counter';
import * as store from './store';

export default Object.assign({},
	directionKeys,
	moduleHot,
	immutable,
	scaleBody,
	statsCounter,
	store,
);
